{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "Node.js variant. Extends the fully-descriptive base and only overrides the environment-specific values (lib, types).",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "target": "es2025",
    "lib": ["es2025"],
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "types": ["node"]
  }
}
